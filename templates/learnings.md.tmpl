# Learnings

Persistent memory across Ralph loop iterations. Append-only — never delete entries.

## Codebase Patterns

Discovered conventions, helpers, and existing code to reuse.

## Signs

Learned constraints that prevent repeated mistakes.
Format: `- **Sign:** [constraint] — discovered iteration N ([what went wrong])`
